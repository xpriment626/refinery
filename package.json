{
  "name": "refinery",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Refinery — local memory service. Ingestion-and-inspection slice (Stage A).",
  "bin": {
    "refinery": "src/cli.ts"
  },
  "scripts": {
    "refinery": "node src/cli.ts",
    "mcp": "node src/mcp.ts",
    "experiment:capture": "node src/experiments/capture.ts",
    "experiment:distillation": "node src/experiments/distillation.ts",
    "experiment:schema": "node src/experiments/schema.ts",
    "experiment:relevance": "node src/experiments/relevance.ts",
    "test": "node --test src/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.67",
    "@mastra/core": "^1.40.0"
  }
}
